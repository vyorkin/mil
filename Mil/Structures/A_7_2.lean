-- 7.2. Algebraic Structures

-- Содержание раздела:
--
-- Как в Mathlib на самом деле устроены алгебраические структуры вроде
-- Group, Ring, Field: они задаются командой class (структура, для
-- которой Lean умеет автоматически находить нужный экземпляр по типу
-- через механизм вывода типовых классов) и связывают тип-носитель с
-- операциями на нём отдельно ("частично упакованный" стиль), а не хранят
-- тип внутри структуры.
--
-- На собственном игрушечном примере (Group₁, Group₂) показывается, как
-- объявление instance регистрирует конкретную реализацию структуры для
-- конкретного типа, и как после этого запись a * b или a⁻¹ начинает
-- работать для этого типа автоматически — поиск экземпляра происходит
-- рекурсивно и прозрачно для пользователя.
--
-- Обсуждается устройство иерархии классов Mathlib для алгебры (Mul, One,
-- Semigroup, Monoid, Group, ...), зачем нужны классы "нотации" (Mul,
-- Zero, Neg и подобные) отдельно от классов "свойств", и класс Inhabited
-- как пример класса, задающего значение по умолчанию, а не операцию.

import Mathlib
