-- 10.4. Matrices, bases and dimension

-- Содержание раздела:
--
-- Явные векторы и матрицы через нотацию ![...]/!![...], матрицы как
-- функции Matrix m n α с произвольным (не обязательно числовым) типом
-- индексов строк и столбцов, стандартные операции (умножение,
-- транспонирование, определитель) и вычисление конкретных примеров через
-- #eval/norm_num.
--
-- Базис Basis ι K V вводится не как список векторов, а как изоморфизм
-- пространства V на "модельное" пространство финитно-носимых функций
-- ι →₀ K, откуда сразу берутся координаты вектора (Basis.repr) и обратно
-- — конструкция базиса из линейно независимого порождающего семейства
-- через Basis.mk.
--
-- Матрица линейного отображения относительно выбранных базисов, замена
-- базиса, и понятие размерности: finrank для конечномерного случая (число
-- элементов базиса) и ранг через мощности (cardinal) в общем случае —
-- обобщение размерности на бесконечномерные пространства.

import Mathlib
