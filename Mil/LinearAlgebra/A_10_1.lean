-- 10.1. Vector spaces and linear maps

-- Содержание раздела:
--
-- Векторное пространство над полем K как экземпляр класса Module K V
-- (модуль — обобщение векторного пространства на произвольное кольцо
-- скаляров), скалярное умножение a • v и связанные с ним леммы (с
-- префиксом smul в имени). Обсуждается тактика module, закрывающая цели,
-- которые следуют из одних только аксиом модуля/векторного пространства,
-- — аналог ring, но для линейной алгебры.
--
-- Линейные отображения вводятся как упакованная структура V →ₗ[K] W (в
-- духе морфизмов из 8.2) с коэрсией к функции, и линейные изоморфизмы
-- V ≃ₗ[K] W. Показывается, как строить линейное отображение — либо явным
-- конструктором с доказательством линейности, либо композицией уже
-- готовых линейных отображений.
--
-- Прямая сумма и произведение семейства векторных пространств: их
-- универсальные свойства (отображение в произведение ⟷ семейство
-- отображений; отображение из прямой суммы ⟷ семейство отображений) и
-- связанные с ними канонические вложения и проекции.

import Mathlib
