-- 10.2. Subspaces and quotients

-- Содержание раздела:
--
-- Подпространства как упакованные подобъекты Submodule K V (частный
-- случай схемы SetLike из 8.3): замкнутость относительно сложения и
-- умножения на скаляр, полная решётка подпространств с ⊓ как
-- пересечением и ⊔ как подпространством, порождённым объединением
-- (span), крайние элементы ⊤ (всё пространство) и ⊥ (нулевое
-- подпространство).
--
-- span K s как наименьшее подпространство, содержащее множество s, с его
-- характеризующим свойством в духе связи Галуа (Galois connection) между
-- "порождает подпространство" и "содержится в подпространстве" — та же
-- идея, что и для span/closure в 8.3, но специализированная на линейную
-- алгебру.
--
-- Образ и прообраз подпространства при линейном отображении (map/comap,
-- обобщение image/preimage из 4.2 на линейные отображения), ядро и образ
-- как частные случаи, и фактор-пространство V / U по подпространству U
-- с канонической проекцией и универсальным свойством поднятия
-- отображений через фактор.

import Mathlib
