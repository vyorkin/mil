-- 11.2. Metric Spaces

-- Содержание раздела:
--
-- Метрические пространства как первая конкретная реализация топологии,
-- заданная функцией расстояния: сходимость, равномерная сходимость,
-- открытые и замкнутые шары как элементарные геометрические объекты, и
-- показ того, что определение непрерывности через фильтры (Tendsto)
-- равносильно привычному ε-δ определению.
--
-- Компактность вводится через свойство Больцано — Вейерштрасса (у любой
-- последовательности есть сходящаяся подпоследовательность), а не сразу
-- через открытые покрытия, — это делает первые доказательства
-- компактности ближе к анализу, знакомому по 3.6. Полнота метрического
-- пространства и фундаментальные последовательности (Коши) — с явными
-- геометрическими (через степени двойки) критериями фундаментальности.
--
-- Автоматизация: тактика continuity для типовых доказательств
-- непрерывности через композицию уже известных непрерывных функций.
-- Отдельно — теорема Бэра о полных метрических пространствах и то, как
-- замкнутость множества характеризуется через пределы сходящихся в нём
-- последовательностей.

import Mathlib
