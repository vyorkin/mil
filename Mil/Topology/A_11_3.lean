-- 11.3. Topological Spaces

-- Содержание раздела:
--
-- Топологическое пространство как обобщение метрического — вводится
-- сразу с двух равносильных сторон: через открытые множества и через
-- окрестности (фильтры окрестностей точки) — раздел показывает переход
-- между этими двумя описаниями и когда какое удобнее.
--
-- Топологии на данном типе образуют полную решётку — обсуждаются
-- pushforward/pullback топологии вдоль функции как способ перенести
-- топологию (то же самое map/comap, что и для фильтров в 11.1, но теперь
-- для топологий целиком), и как из этого строятся топология
-- факторпространства и топология произведения.
--
-- Аксиомы отделимости (T₂/хаусдорфовость, T₃/регулярность) как условия,
-- гарантирующие единственность пределов, первая аксиома счётности,
-- связывающая топологию с последовательностями (когда предельных точек
-- достаточно описывать через последовательности, а не через более общие
-- фильтры), и компактность через предельные точки фильтров как
-- обобщение открытых покрытий.

import Mathlib
