-- 8.1. Basics

-- Содержание раздела:
--
-- Продолжение темы 7.2 вглубь: как устроено наследование между классами
-- через extends, почему у Monoid, который расширяет одновременно
-- Semigroup и класс с единицей, возникает риск "плохого ромба" — два
-- разных пути получить одно и то же поле, дающих не обязательно
-- совпадающие по построению (хотя и равные по значению) термы — и как
-- Mathlib избегает этой проблемы через явные значения полей по умолчанию
-- ("забывчивое" наследование).
--
-- На собственных игрушечных классах (One₁, Dia₁ и их надстройках
-- Semigroup, Monoid, Group) пошагово собирается алгебраическая иерархия,
-- аналогичная мультипликативной части иерархии Mathlib, чтобы разобраться,
-- как устроен реальный Monoid/Group/Ring изнутри.
--
-- Обсуждается атрибут to_additive, который по мультипликативному
-- определению автоматически порождает аддитивный аналог (Monoid →
-- AddMonoid и подобное) — практический ответ на то, откуда в Mathlib
-- берутся параллельные аддитивная и мультипликативная версии одной и той
-- же иерархии классов, о которой шла речь в 2.2.

import Mathlib
