-- 13.1. Elementary Integration

-- Содержание раздела:
--
-- Интеграл элементарных функций по конечному отрезку вещественной прямой
-- и обе части основной теоремы анализа: производная интеграла с
-- переменным верхним пределом равна подынтегральной функции, а интеграл
-- производной выражается через значения первообразной на концах отрезка
-- (формула Ньютона — Лейбница).
--
-- На конкретных примерах (многочлены, 1/x) показывается вычисление
-- интегралов через уже известные первообразные, и как из
-- дифференцирования функции, заданной интегралом по переменному пределу,
-- получаются новые факты о самой подынтегральной функции.
--
-- Свёртка (convolution) двух функций как интегральное преобразование —
-- первый пример того, как интеграл используется не только для вычисления
-- площади, но и как инструмент построения новых функций из старых.

import Mathlib
