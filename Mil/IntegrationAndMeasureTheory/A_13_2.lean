-- 13.2. Measure Theory

-- Содержание раздела:
--
-- σ-алгебры и измеримые множества как формализация того, что вообще
-- можно измерить: замкнутость относительно дополнения и счётного
-- объединения/пересечения, измеримость пустого и универсального
-- множества как немедленные следствия аксиом.
--
-- Мера как функция на измеримых множествах, счётная аддитивность на
-- дизъюнктных измеримых множествах, построение мер на произведении
-- пространств. Класс типов Encodable как формализация "счётности" в
-- прикладном, вычислительном смысле, нужная для работы со счётными
-- объединениями.
--
-- Свойства, выполняющиеся "почти всюду" (относительно меры),
-- формализуются через фильтр дополнений множеств меры ноль — снова
-- аппарат фильтров из 11.1, но теперь применённый к мере, а не к
-- топологии, что показывает фильтры как общий язык для разных
-- представлений понятия "почти" или "предельно".

import Mathlib
