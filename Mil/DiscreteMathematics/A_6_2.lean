-- 6.2. Counting Arguments

-- Содержание раздела:
--
-- Комбинаторные рассуждения о мощности конечных множеств: card
-- дизъюнктного объединения — это сумма card, card произведения — это
-- произведение card, card образа при инъективном отображении равен card
-- прообраза. Показывается, как из этих простых тождеств тактиками simp,
-- rw и omega собираются более сложные подсчёты.
--
-- На примере точек треугольной решётки и биекций между Finset и типами
-- Σ (сигма-типами) демонстрируется приём "пересчитать одно и то же
-- множество двумя способами" — стандартный комбинаторный трюк,
-- формализуемый как построение явной биекции или как два разных
-- выражения для одного и того же card.
--
-- Отдельно разбирается принцип Дирихле (pigeonhole: если |A| > |B| и
-- f : A → B, то f не инъективна) и его применение к задачам о
-- последовательных числах и взаимной простоте — типичный пример, где
-- само утверждение тривиально по card, а трудность в правильном выборе
-- множеств A и B.

import Mathlib
